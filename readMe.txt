Full Setup & Run Commands
# clone the repo
git clone https://github.com/stevenallendev/expense-tracker.git
cd expense-tracker

Backend (API)
cd server
npm install
npm run dev


Leave this terminal running

Frontend (React App)
cd ../client
npm install
npm run dev

What npm install does

Downloads all required dependencies listed in package.json

Creates the node_modules folder

Must be run once per folder (server and client)

App URLs
Frontend: http://localhost:5173
Backend:  http://localhost:4000

Common issues
# stop a running process
Ctrl + C

# reinstall dependencies if something is weird
rm -rf node_modules
npm installFull Setup & Run Commands
# clone the repo
git clone https://github.com/stevenallendev/expense-tracker.git
cd expense-tracker

Backend (API)
cd server
npm install
npm run dev


Leave this terminal running

Frontend (React App)
cd ../client
npm install
npm run dev

What npm install does

Downloads all required dependencies listed in package.json

Creates the node_modules folder

Must be run once per folder (server and client)

App URLs
Frontend: http://localhost:5173
Backend:  http://localhost:4000

Common issues
# stop a running process
Ctrl + C

# reinstall dependencies if something is weird
rm -rf node_modules
npm install